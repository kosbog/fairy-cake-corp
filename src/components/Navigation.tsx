import * as React from 'react';

interface IProps {
    menuList: string[];
}

const Navigation = ({ menuList }: IProps) => {
    return (
        <header>
            test
        </header>
    );
};

export default Navigation;
